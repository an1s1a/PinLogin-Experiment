<?xml version="1.0" encoding="utf-8"?>
<!-- LAYOUT NON PIU' UTILIZZATO, QUESTO ERA QUELLO CHE AVEVI FATTO TU, ADESSO VIENE UTILIZZATO INCLUDE_DOT
CHE HA AL SUO INTERNO SOLAMENTE UN VIEWSWITCHER CHE VIENE AGGIUNTO IN MODO PROGRAMMATO NEL CODICE JAVA DELLE
CLASSI ALLE QUALI SERVE -->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:id="@+id/insert_pin_base">

    <LinearLayout
        android:orientation="horizontal"
        android:id="@+id/linear_layout_small_circles"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="64dp">

        <!--<ViewSwitcher
            android:id="@+id/first_viewswitcher"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:inAnimation="@anim/fade_in"
            android:outAnimation="@anim/fade_out">

            <ImageView
                android:id="@+id/first_ring"
                android:layout_width="18dp"
                android:layout_height="18dp"
                android:src="@drawable/small_ring" />

            <ImageView
                android:id="@+id/first_circle"
                android:layout_width="18dp"
                android:layout_height="18dp"
                android:src="@drawable/small_circle" />

        </ViewSwitcher>-->

    </LinearLayout>

</RelativeLayout>